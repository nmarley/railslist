
* tests for files
  - users can only view their own files in files#index page
  - users can only see their own files when visiting the specific file's url
  - users can create files via page (also flesh out Attachment model specs)

* shared lists (one of the main used functions)

* open-source this for real

* add an "undo" action to item deletes

* use s3 storage for files

* tests. lots of tests.

* (admin?) settings to turn on/off new user registrations

