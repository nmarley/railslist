<%= provide(:title, @list.name) %>
<h1><%= @list.name %></h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@list) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= f.label :name %>
    <% end %>
  </div>
</div>
